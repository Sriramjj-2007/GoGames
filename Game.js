var block = document.getElementById('block')
document.addEventListener('keydown', (event) => {
    var name = event.key;
    // var code = event.code;

    switch (name) {
        default:
            console.log(name);
            break;
    }
}, false);